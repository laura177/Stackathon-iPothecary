import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

export default function MyMeds(){
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Hello This is Your Meds Manager</Text>
      </ScrollView>
    </SafeAreaView>
  )
}
